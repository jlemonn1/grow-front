# Copia este archivo como .env.local para desarrollo local
# O configura estas variables en Vercel para producción
#
# IMPORTANTE: Todas las variables son obligatorias. No hay valores por defecto hardcodeados.

# URL base de la API del backend (incluye /api/v1)
# En desarrollo local: http://localhost:8080/api/v1
# En producción: https://api.tudominio.com/api/v1
# Esta variable se usa para todas las llamadas a la API REST
VITE_API_URL=http://localhost:8080/api/v1

# URL base del servidor (sin /api/v1) - OPCIONAL
# Solo necesaria si el servidor está en un dominio/puerto diferente al de la API
# Si no se define, se deriva automáticamente de VITE_API_URL removiendo /api/v1
# Útil para recursos estáticos como imágenes, logos, etc.
# Ejemplo: http://localhost:8080
# VITE_SERVER_URL=http://localhost:8080
